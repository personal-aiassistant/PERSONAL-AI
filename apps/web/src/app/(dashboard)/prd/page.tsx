import { PrdGenerator } from "@/modules/generators/prd/prd-generator";

export const metadata = { title: "PRD Generator — CodeForge AI" };

export default function PrdPage() {
  return <PrdGenerator />;
}
