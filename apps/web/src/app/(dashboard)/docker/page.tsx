import { DockerGenerator } from "@/modules/generators/docker/docker-generator";

export const metadata = { title: "Docker Generator — CodeForge AI" };

export default function DockerPage() {
  return <DockerGenerator />;
}
