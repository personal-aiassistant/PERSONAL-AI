import { AdminContent } from "@/modules/admin/components/admin-content";

export const metadata = { title: "Admin Dashboard — CodeForge AI" };

export default function AdminPage() {
  return <AdminContent />;
}
