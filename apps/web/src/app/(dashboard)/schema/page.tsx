import { SchemaGenerator } from "@/modules/generators/schema/schema-generator";

export const metadata = { title: "Schema Generator — CodeForge AI" };

export default function SchemaPage() {
  return <SchemaGenerator />;
}
