import { ApiBuilder } from "@/modules/generators/api-builder/api-builder";

export const metadata = { title: "API Builder — CodeForge AI" };

export default function ApiBuilderPage() {
  return <ApiBuilder />;
}
