import { WorkspaceContent } from "@/modules/workspace/workspace-content";

export const metadata = { title: "Workspace — CodeForge AI" };

export default function WorkspacePage() {
  return <WorkspaceContent />;
}
